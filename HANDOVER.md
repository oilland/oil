# 📋 HANDOVER — وضعیت پروژه روغن‌لند (OilLand)

> **آخرین به‌روزرسانی: ۲۰۲۶/۰۸/۲۷ — نسخه ۸ (قفل محتوا/عکس + هوک گیت‌هاب + CONTINUE.md)**
> ریپو: `oilland/oil` · سایت لایو: روی **لیارا** بالاست (دامنه هدف: oilland.shop)
> ⚡ **برای ادامه کار در چت جدید:** فایل `CONTINUE.md` را بده و بگو از همان‌جا شروع کن. این پروژه Versus نیست.

---

## 🔄 ادامه از اینجا (وضعیت لحظه‌ای)

**کارهای انجام‌شده تا الان:**
1. ✅ بررسی کامل پروژه + اجرای موفق لوکال (Postgres 17، سید ۱۷۶ محصول) — نسخه ۱
2. ✅ تأیید شد کارت به کارت به‌طور کامل پیاده است (CheckoutForm + api/orders + تنظیمات ادمین + تأیید دستی پرداخت در جزئیات سفارش) — نسخه ۲
3. ✅ **دستیار سئو هوشمند ساخته شد** (جزئیات پایین) و لوکال تست شد: امتیاز سئو ۷۳←۹۰، بیلد پروداکشن سبز — نسخه ۳
4. ✅ **اسکریپت گیت‌هاب** اضافه شد: `npm run push` (فایل `scripts/push.js`) — add + commit + push یک‌کلیکی — نسخه ۴
5. ✅ **خروجی زیپ برای لیارا** ساخته شد: `oilland-liara.zip` (بدون node_modules/.next/.env — آماده درگ روی لیارا) — نسخه ۴
6. ✅ ~~`package-lock.json` تولید شد~~ ← **هشدار مهم:** روی لیارا `package-lock.json` نباید باشد! زیپ اول به‌خاطر همین خطای `npm ci: Exit handler never called` داد (آدرس‌های npmjs داخل lock با میرور ایرانی لیارا تداخل دارد — به همین دلیل از قبل در `.gitignore` هم بوده). زیپ بدون lock ساخته شد و Dockerfile هم به `npm ci || npm install` مقاوم شد
7. ✅ **رفع خطای بیلد لیارا (نسخه ۶):** `sitemap.ts` موقع بیلد به دیتابیس وصل می‌شد و چون در محیط بیلد لیارا دیتابیس در دسترس نیست، بیلد می‌شکست (`Can't reach database server at oilshop-db:5432` هنگام prerender سایت‌مپ). راه‌حل: `export const dynamic = 'force-dynamic'` + try/catch با fallback به URLهای ثابت. **تضمین:** بیلد پروداکشن با دیتابیس کاملاً خاموش تست شد و سبز بود (دقیقاً شرایط لیارا)
8. 🌐 کاربر پروژه را روی لیارا دیپلوی کرده و سایت لایو است
9. ✅ **نسخه ۷ (۲۰۲۶/۰۸/۲۷):**
   - رفع کرش `/products?sort=bestseller` (مشاهده همه پرفروش‌ترین‌ها) — Prisma فقط یک فیلد در شیء orderBy قبول می‌کرد
   - ماندگاری عکس آپلودی در PostgreSQL (`MediaFile`) تا با دیپلوی/ری‌استارت لیارا نپرد — فشرده‌سازی sharp از قبل فعال بود
   - فوتر فروشگاه + پنل ادمین: «طراحی و توسعه: seytare team»
   - ⚠️ عکس‌هایی که قبل از نسخه ۷ فقط روی دیسک موقت بودند اگر پریده‌اند قابل بازیابی نیستند؛ آپلودهای بعدی در DB می‌مانند.
10. ✅ **نسخه ۸:** سید روی دیتابیس زنده اجرا نمی‌شود (اگر user/product/banner/post/media باشد). آپلود بدون ذخیره در DB رد می‌شود. بوت، فایل‌های MediaFile را روی دیسک برمی‌گرداند. اسکریپت `npm run push` + هوک `.githooks/post-commit` برای پوش خودکار. فایل `CONTINUE.md` برای چت جدید.

**کار بعدی پیشنهادی (به ترتیب):**
- [ ] درگ کردن `oilland-liara.zip` روی لیارا (بدون package-lock) → دیپلوی نسخه ۸
- [ ] بعد از دیپلوی: اگر اسلایدر خالی است عکس را دوباره آپلود کن (از این به بعد نمی‌پرد)؛ تست `/products?sort=bestseller`
- [ ] ثبت سایت در Google Search Console + ارسال `https://oilland.shop/sitemap.xml`
- [ ] اتصال درگاه زرین‌پال (هنوز پیاده نشده — فقط کارت‌به‌کارت/COD/mock فعال است)
- [ ] اتصال SMS (کاوه‌نگار) برای اعلان سفارش
- [ ] گرفتن کلید AI (مثلاً Liara AI) و واردکردنش در «دستیار سئو» برای مقالات باکیفیت‌تر

**ابزار دیپلوی (نسخه ۴):**
- `npm run push` یا `npm run push -- "پیام"` → ارسال تغییرات به گیت‌هاب (اگر لیارا به ریپو وصل باشد = دیپلوی خودکار). بار اول اگر ریموت نبود: `git remote add origin https://github.com/oilland/oil.git`
- زیپ دیپلوی لیارا: `zip -r oilland-liara.zip oil -x "oil/node_modules/*" "oil/.next/*" "oil/.git/*" "oil/.env" "oil/package-lock.json"` — **هرگز package-lock.json را داخل زیپ لیارا نگذار!**


---

## 🤖 دستیار سئو (جدید — نسخه ۳)

**مسیر در پنل:** `/admin/seo` (سایدبار → محتوا → «دستیار سئو») — دسترسی: `settings.manage`

**قابلیت‌ها:**
- **تحلیل لحظه‌ای:** امتیاز سئو ۰–۱۰۰ + لیست کم‌وکاستی‌ها (محصولات بدون متا، توضیحات کوتاه، وبلاگ قدیمی/کم‌محتوا، صفحات بدون متا، تنظیمات سایت) با برچسب «رفع خودکار می‌شود / اقدام دستی»
- **دکمه اجرا با دو حالت:**
  - «اجرا + بازبینی مدیر» → مقاله جدید به‌صورت **پیش‌نویس** ذخیره می‌شود (از /admin/blog تأیید/ویرایش کن)
  - «اجرای تمام‌خودکار» → مقاله **مستقیم منتشر** می‌شود
- **اتوپایلوت در هر اجرا:** تکمیل متادیتای سئوی همه محصولات/مقالات/صفحات + بازنویسی توضیحات کوتاه محصولات (از روی مشخصات فنی) + تولید ۱ مقاله وبلاگ سئوشده + گزارش
- **تولید مقاله با موضوع دلخواه:** فرم جدا در همان صفحه
- **خروجی:** «دانلود گزارش کامل (Markdown)» از `/api/seo/report`
- **موتور محتوا:** اگر کلید AI (سازگار با OpenAI — Liara AI/AvalAI/OpenAI) در همان صفحه ثبت شود از AI استفاده می‌کند؛ وگرنه **قالب هوشمند داخلی** (بدون هیچ وابستگی) کار می‌کند
- **اجرای زمان‌بندی‌شده:** `GET /api/seo/auto?key=<AUTH_SECRET>` — برای کرون لیارا یا هر پینگر خارجی؛ حالت آن از «حالت پیش‌فرض اجرا» در تنظیمات صفحه خوانده می‌شود
- **بونوس سئو:** `sitemap.xml` و `robots.txt` استاندارد اضافه شد (قبلاً اصلاً وجود نداشت!)

**فایل‌های جدید/تغییریافته (برای کامیت):**
```
جدید:  src/lib/seo/ai.ts                        ← کلاینت AI سازگار با OpenAI
جدید:  src/lib/seo/analyzer.ts                  ← موتور تحلیل و امتیازدهی سئو
جدید:  src/lib/seo/autopilot.ts                 ← اتوپایلوت: رفع خودکار + تولید مقاله + گزارش
جدید:  src/actions/admin-seo.ts                 ← اکشن‌های سرور (اجرا/تولید مقاله/تنظیمات)
جدید:  src/app/admin/(panel)/seo/page.tsx       ← صفحه دستیار در پنل ادمین
جدید:  src/app/api/seo/report/route.ts          ← دانلود گزارش Markdown (ادمین)
جدید:  src/app/api/seo/auto/route.ts            ← اجرای زمان‌بندی‌شده (با AUTH_SECRET)
جدید:  src/app/sitemap.ts                       ← نقشه سایت پویا
جدید:  src/app/robots.ts                        ← robots.txt
ویرایش: src/lib/settings.ts                     ← کلیدهای aiBaseUrl/aiApiKey/aiModel/seoAutoMode
ویرایش: src/components/admin/Sidebar.tsx        ← لینک «دستیار سئو»
```
> نیازی به مایگریشن دیتابیس نیست — همه‌چیز از جدول‌های موجود (SiteSetting/BlogPost/…) استفاده می‌کند.

**نتیجه تست لوکال:** اجرای اتوپایلوت → امتیاز ۷۳←۹۰ · ۱۷۶ توضیح محصول بازنویسی · ۴ متای مقاله + ۴ متای صفحه تکمیل · مقاله «راهنمای کامل انتخاب روغن موتور مناسب خودرو» در پیش‌نویس · بیلد پروداکشن بدون خطا.

---

## ۱) پروژه چیست؟


فروشگاه اینترنتی روغن و روانکار خودرو/صنعتی (برند بهران) — کاملاً فارسی و RTL:

- **استک:** Next.js 15 (App Router) · TypeScript · Tailwind CSS 3 · Prisma 6 · PostgreSQL · Redis (اختیاری)
- **دیتا:** ۱۷۶ محصول واقعی بهران (۱۴۱ صنعتی + ۳۵ موتوری) در `data/*.json` + سید کامل
- **فروشگاه:** صفحه اصلی (بنر، دسته‌ها، پرفروش‌ها، ویزارد سازگاری خودرو)، کاتالوگ با فیلتر/جستجو/مرتب‌سازی، صفحه محصول (گالری، مشخصات، نظرات، JSON-LD)، سبد خرید (localStorage)، علاقه‌مندی، کوپن، چک‌اوت چندمرحله‌ای، حساب مشتری، وبلاگ، صفحات CMS، خبرنامه
- **پنل ادمین:** داشبورد KPI + نمودار فروش ۳۰روزه، مدیریت کامل محصول/دسته/برند/بنر/کوپن/سفارش/مشتری/وبلاگ/نقش‌ها و دسترسی‌ها (RBAC)/لاگ فعالیت/تنظیمات سراسری
- **زیرساخت:** ۳۶ مدل Prisma (شامل MediaFile)، احراز هویت JWT (کوکی httpOnly + middleware)، bcrypt، zod، کش Redis با fallback حافظه، Dockerfile + دو docker-compose (پروداکشن و دمو)، بوت‌استرپ خودکار دیتابیس (instrumentation + `/api/setup`)

**حساب‌های دمو:** ادمین `admin@example.com / admin123` · مشتری `customer@example.com / customer123`

---

## ۲) وضعیت فعلی — ✅ سالم و قابل اجرا

تست عملی انجام شد (Node 20 + PostgreSQL 17):

| مرحله | نتیجه |
|---|---|
| `npm install` | ✅ بدون خطا |
| `npx prisma migrate deploy` | ✅ مایگریشن init اعمال شد |
| `npm run db:seed` | ✅ ۱۷۶ محصول + ۲۱ دسته + محتوا |
| `next dev` | ✅ بالا آمد |
| صفحات `/`، `/products`، `/admin/login` | ✅ HTTP 200 |
| `/api/health` | ✅ `{"database":"connected","products":176}` |

نتیجه: **پروژه از نظر فنی کامل و اجراشدنی است؛ در مرحله «آماده استقرار + اتصال درگاه پرداخت» قرار دارد.**

---

## ۳) روش اجرا

### الف) توسعه محلی (PostgreSQL)
```bash
# ۱. یک PostgreSQL بالا بیاور و دیتابیس/کاربر oilshop بساز
# ۲. فایل .env بساز (از .env.example کپی کن) — حداقل:
#    DATABASE_URL="postgresql://oilshop:oilshop@localhost:5432/oilshop?schema=public"
#    AUTH_SECRET="یک-رشته-تصادفی-طولانی"
npm install
npx prisma migrate deploy   # یا: npx prisma migrate dev
npm run db:seed
npm run dev                 # http://localhost:3000
```

### ب) پروداکشن با Docker
```bash
docker compose up -d --build   # Postgres 16 + Redis 7 + اپ (پورت 3000)
```
entrypoint خودش مایگریشن و سید idempotent را اجرا می‌کند.

### ج) دموی سریع (تک‌کانتینر SQLite)
```bash
docker compose -f docker-compose.demo.yml up -d --build
```
⚠️ فعلاً **خراب است** — بخش «مشکلات» را ببین.

### د) لیارا
راهنمای گام‌به‌گام فارسی در `DEPLOY.md` (دامنه هدف: `oilland.shop`).

---

## ۴) مشکلات و نکات شناسایی‌شده

1. **🔴 دموی SQLite کار نمی‌کند:** `docker-compose.demo.yml` مقدار `DATABASE_URL=file:/data/dev.db` می‌دهد، اما `prisma/schema.prisma` روی `provider = "postgresql"` قفل است ⇒ `prisma db push` در entrypoint شکست می‌خورد. (ادعای README درباره «SQLite در دمو» با کد فعلی ناسازگار است.)
   **راه‌حل:** یا دموی SQLite را حذف کن، یا یک `schema.sqlite.prisma` جدا نگه دار و در entrypoint سوییچ کن.
2. **🟠 درگاه پرداخت واقعی وصل نیست:** پرداخت فعلاً mock/کارت‌به‌کارت/در محل است (`gateway: 'mock'` در `api/orders`). مدل `Payment` و تنظیمات آماده‌اند ولی زرین‌پال/آیدی‌پی پیاده‌سازی نشده.
3. **🟠 ایمیل/SMS پیاده نشده:** متغیرهای SMTP/SMS در `.env.example` هستند ولی هیچ ارسالی در کد نیست (مدل `Notification` فقط تعریف شده).
4. **🟢 آپلود ماندگار (نسخه ۷):** فایل علاوه بر دیسک در جدول `MediaFile` (PostgreSQL) ذخیره می‌شود و روت `/uploads/...` اول دیسک بعد دیتابیس را می‌خواند. عکس‌های آپلودشده **بعد از این نسخه** با دیپلوی نمی‌پرند. عکس‌های قدیمی که فقط روی دیسک کانتینر بودند قابل بازیابی نیستند.
5. **🟡 امنیت پیش‌فرض:** رمز ادمین `admin123` و `AUTH_SECRET` پیش‌فرض در compose — قبل از لایو حتماً عوض شوند. هدر `X-Frame-Options` هم عمداً حذف شده (برای پیش‌نمایش) و باید در پروداکشن برگردد.
6. **🟡 موارد ناتمام طبق Roadmap:** ۲FA ادمین، سینک سروری سبد/علاقه‌مندی برای کاربران لاگین، UI واریانت محصول، UI ادمین برای FAQ/Testimonial.
7. **⚪ جزئی:** نام فایل‌های SVG محصولات فارسی است (ریسک encoding روی بعضی سیستم‌ها/CDNها)؛ تست خودکار (unit/e2e) وجود ندارد؛ `package-lock.json` در ریپو نیست (بیلد Docker با `npm ci` به آن نیاز دارد — `npm ci` بدون lock خطا می‌دهد ⇒ یک بار `npm install` بزن و lock را کامیت کن).

---

## ۵) قدم بعدی (به ترتیب اولویت)

1. **کامیت `package-lock.json`** (رفع خطای `npm ci` در Dockerfile) و رفع تکلیف دموی SQLite (بند ۴-۱).
2. **دیپلوی روی لیارا** طبق `DEPLOY.md`: ساخت اپ + دیتابیس PostgreSQL + ست‌کردن env ها + اتصال دامنه `oilland.shop` و SSL. یک **Disk** هم برای مسیر آپلودها بگیر.
3. بلافاصله بعد از بالا آمدن: **تغییر رمز ادمین + AUTH_SECRET واقعی** و ورود اطلاعات واقعی فروشگاه در تنظیمات.
4. **اتصال زرین‌پال** (مرچنت‌کد از zarinpal.com) — ساخت `src/lib/payments/zarinpal.ts` + روت‌های request/verify + آپدیت چک‌اوت.
5. اتصال **SMS (کاوه‌نگار/ملی‌پیامک)** برای اعلان سفارش، سپس SMTP.
6. تکمیل موارد Roadmap (۲FA، سینک سبد، واریانت‌ها) و افزودن چند تست e2e برای مسیر خرید.

---

*این پروژه مستقل است و هیچ ارتباطی با پروژه Versus ندارد (اکانت کامیت‌کننده صرفاً یک اکانت شخصی گیت‌هاب است).*
