# 🚀 راهنمای دیپلوی فروشگاه روغن‌لند روی لیارا

> دامنه: **oilland.shop** · هاست: لیارا · دیتابیس: PostgreSQL (مدیریت‌شده لیارا)

---

## ۰) پیش‌نیازها (چیزهایی که باید از قبل داشته باشی)

- [x] دامنه `oilland.shop` (از nic.ir)
- [x] حساب گیت‌هاب
- [ ] حساب لیارا ← **https://console.liara.ir** (قدم بعدی تو!)

---

## ۱) ساخت حساب لیارا

1. برو به **https://console.liara.ir** و با شماره موبایل/ایمیل ثبت‌نام کن
2. ۵۰۰ هزار تومان اعتبار هدیه می‌گیری (برای شروع کافیه)
3. فعلاً هیچی نساز — فقط حساب

---

## ۲) آپلود پروژه روی گیت‌هاب (روی سیستم خودت)

```bash
# پروژه رو از ورک‌اسپیس دانلود کن (یا کپی کن) و بعد:
cd oil-shop
git init
git add .
git commit -m "فروشگاه روغن‌لند"
git branch -M main
git remote add origin https://github.com/USERNAME/oil-shop.git
git push -u origin main
```

> `USERNAME` = اسم کاربری گیت‌هابت. قبل از push حتماً ریپوی جدید `oil-shop` رو توی گیت‌هاب بساز (دکمه سبز New repository).

---

## ۳) دیپلوی روی لیارا

1. توی کنسول لیارا: **«ساخت برنامه» (Create App)**
2. پلتفرم: **«میزبانی برنامه» (Platform/PaaS)**
3. گزینهٔ اتصال به **GitHub** رو بزن و ریپوی `oil-shop` رو انتخاب کن
4. تنظیمات برنامه:
   - **Build command:** `npm install && npx prisma generate && npm run build`
   - **Run command / Start:** `npm start`
   - پورت: `3000`
5. **متغیرهای محیطی (Environment Variables)** رو اضافه کن:

| کلید | مقدار |
|---|---|
| `DATABASE_URL` | آدرس دیتابیسی که در قدم ۴ می‌سازی |
| `AUTH_SECRET` | یک رشته تصادفی طولانی (مثلاً ۴۰ کاراکتر) |
| `APP_URL` | `https://oilland.shop` |
| `NEXT_PUBLIC_APP_URL` | `https://oilland.shop` |

6. دیپلوی کن

---

## ۴) ساخت دیتابیس PostgreSQL

1. توی کنسول لیارا: **«ساخت دیتابیس» (Create Database)**
2. نوع: **PostgreSQL**
3. بعد از ساخته شدن، لیارا یک **آدرس اتصال** (مثل `postgresql://user:pass@host:5432/db`) بهت می‌ده
4. این آدرس رو توی متغیر `DATABASE_URL` برنامه بذار و دیپلوی مجدد کن

---

## ۵) ساخت جدول‌ها و وارد کردن داده‌ها

اولین بار باید یک‌بار این دو دستور رو اجرا کنی. ساده‌ترین راه:

- توی لیارا، از تب **Console / Terminal** برنامه:
  ```bash
  npx prisma migrate deploy
  npx tsx prisma/seed.ts
  ```

> این کار جدول‌ها رو می‌سازه و **۱۷۶ محصول بهران + دسته‌بندی‌ها + تنظیمات + ادمین** رو وارد می‌کنه.

---

## ۶) اتصال دامنه oilland.shop

1. توی لیارا: تب **«دامنه‌ها» (Domains)** برنامه
2. دامنه `oilland.shop` رو اضافه کن
3. لیارا بهت دو تا **NS** یا رکورد DNS می‌ده
4. برو توی پنل nic.ir (دامنه‌های من ← oilland.shop ← مدیریت DNS) و اون رکوردها رو ثبت کن
5. گواهی SSL خودکار لیارا فعال می‌شه → سایت روی `https://oilland.shop` بالا میاد

---

## ۷) بعد از بالا آمدن

1. وارد پنل مدیریت شو: `https://oilland.shop/admin` → `admin@example.com` / `admin123`
2. **اولین کار: رمز ادمین رو عوض کن** و اطلاعات واقعی فروشگاه رو وارد کن (تنظیمات)
3. باگ‌گیری و درگاه پرداخت رو ادامه می‌دیم

---

## 📌 نکات مهم

- **عکس‌های آپلودی:** از نسخه ۷ به بعد داخل PostgreSQL ذخیره می‌شوند (جدول `MediaFile`) تا با دیپلوی لیارا نپرند. فشرده‌سازی هوشمند (sharp) هم فعال است.
- **حجم:** کل سایت سبکه (تصاویر WebP/SVG)، برای پلن پایه لیارا کافیه
- **بکاپ:** دیتابیس مدیریت‌شده لیارا خودش بکاپ می‌گیره
- **آپدیت‌ها:** هر تغییری که بزنیم، تو push می‌کنی و لیارا خودکار دیپلوی می‌کنه
- **درگاه پرداخت:** بعد از بالا آمدن سایت، زرین‌پال رو وصل می‌کنیم (مرچنت‌کد از zarinpal.com)
